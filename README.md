use `python main.py --model [your model] --epochs [epoch] --learning_rate [lr] --mode train` to train your model.
you can use the environment follow the environment.yml or use `conda env create -f environment.yml`
